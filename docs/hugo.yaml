baseURL: https://deekue.github.io/linkrouter
locale: en-us
title: LinkRouter
theme: hugo-coder
caches:
  images:
    dir: :cacheDir/images
disableKinds:
  - taxonomy
  - term

module:
  mounts:
    - source: assets
      target: assets

    - source: data
      target: data
    - source: ../app
      target: data/app
      files:
      - linkrouter-rules-examples.json
    - source: static
      target: static
    - source: ..
      target: static/repo
      files:
      - 'LICENSE'

    # Mount external Markdown files into your content tree
    - source: content
      target: content
    - source: ..
      target: content/repo
      files:
      - '*.md'

# hugo-coder config
markup:
  highlight:
    style: "github-dark"
params:
  author: "deekue"
  info: "LinkRouter"
  description: "LinkRouter"
  keywords: "linkrouter,android"
  colorScheme: "auto"
