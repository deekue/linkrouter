package com.linkrouter

object LinkRouter {
    const val EXTRA_HANDLED = "com.linkrouter.extra.HANDLED"
    const val LOOP_GUARD_PARAM = "__lr"

    /** Carries the URL to open through the in-app [BrowserChooserActivity]. */
    const val EXTRA_URI = "com.linkrouter.extra.URI"
}
